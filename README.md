Prerequisites:

- Windows 10 or later
- 7-Zip installed & "7z.exe" in the PATH

Usage:
```
unzipper.exe <path_to_zip_file>
```

This will:
- extract the zip file to a folder with the same name as the zip file
- open the folder in the default file explorer
- open a new explorer window if no window already exists